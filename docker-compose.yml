services:
  croma-sync:
    build: .
    restart: unless-stopped
    ports:
      - "5858:5858"
    volumes:
      # El SQLite de cada room persiste aquí y sobrevive a los redeploys.
      - croma-sync-data:/data

volumes:
  croma-sync-data:
